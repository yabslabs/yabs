## This block specifies which API Version to use and what kind of object this is
apiVersion: alpha/v1
kind: Job
metadata:
  name: github.com restore prod to test
  namespace: restore-staging
spec: 
  runner:
    selector:
      name:
  stages:
    - restore
  task 1:
    stage: restore
    source: "{backupStorage}.production"
    provider: "{provider}.staging"